<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="style.css">
    <title>College</title>
</head>
<body>
    <div class="row">
        <div class="col-8 offset-2 main">
        <div class="flex">
            <h1>Students Data</h1>
            <a href="/" class="btn btn-primary">Add Data</a>
        </div>

        <% for(let student of allStudent){ %>
            <div class="box">
                <p><span class="heading">Name: </span><%= student.name %></p>
                <p><span class="heading">Roll No: </span><%= student.rollno %></p>
                <p><span class="heading">Address: </span><%= student.address %></p>
                <p><span class="heading">Course Name: </span><%= student.courseName %></p>
            </div>
        <% } %>
        </div>    
    </div>
</body>
</html>